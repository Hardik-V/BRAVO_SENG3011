name: CI/CD

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: pytest --cov=. --cov-report=term-missing
      - run: flake8 .
      - run: mypy .

  deploy-dev:
    needs: test
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ap-southeast-2

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Deploy Collection Lambda
        run: |
          cd collection
          pip install -r requirements.txt -t package/
          cp handler.py package/
          cd package && zip -r ../deployment.zip .
          cd ..
          aws s3 cp deployment.zip s3://${{ secrets.AWS_BUCKET_NAME }}/dev/deployments/collection.zip
          aws lambda update-function-code \
            --function-name bravo-collection-dev \
            --s3-bucket ${{ secrets.AWS_BUCKET_NAME }} \
            --s3-key dev/deployments/collection.zip

      - name: Deploy Retrieval Lambda
        run: |
          cd retrieval
          pip install -r requirements.txt -t package/
          cp handler.py package/
          cd package && zip -r ../deployment.zip .
          cd ..
          aws s3 cp deployment.zip s3://${{ secrets.AWS_BUCKET_NAME }}/dev/deployments/retrieval.zip
          aws lambda update-function-code \
            --function-name bravo-retrieval-dev \
            --s3-bucket ${{ secrets.AWS_BUCKET_NAME }} \
            --s3-key dev/deployments/retrieval.zip

      - name: Deploy Visualisation Lambda
        run: |
          cd visualisation
          pip install -r requirements.txt -t package/
          cp handler.py package/
          cd package && zip -r ../deployment.zip .
          cd ..
          aws s3 cp deployment.zip s3://${{ secrets.AWS_BUCKET_NAME }}/dev/deployments/visualisation.zip
          aws lambda update-function-code \
            --function-name bravo-visualisation-dev \
            --s3-bucket ${{ secrets.AWS_BUCKET_NAME }} \
            --s3-key dev/deployments/visualisation.zip

  deploy-prod:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ap-southeast-2

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Deploy Collection Lambda
        run: |
          cd collection
          pip install -r requirements.txt -t package/
          cp handler.py package/
          cd package && zip -r ../deployment.zip .
          cd ..
          aws s3 cp deployment.zip s3://${{ secrets.AWS_BUCKET_NAME }}/prod/deployments/collection.zip
          aws lambda update-function-code \
            --function-name bravo-collection-prod \
            --s3-bucket ${{ secrets.AWS_BUCKET_NAME }} \
            --s3-key prod/deployments/collection.zip

      - name: Deploy Retrieval Lambda
        run: |
          cd retrieval
          pip install -r requirements.txt -t package/
          cp handler.py package/
          cd package && zip -r ../deployment.zip .
          cd ..
          aws s3 cp deployment.zip s3://${{ secrets.AWS_BUCKET_NAME }}/prod/deployments/retrieval.zip
          aws lambda update-function-code \
            --function-name bravo-retrieval-prod \
            --s3-bucket ${{ secrets.AWS_BUCKET_NAME }} \
            --s3-key prod/deployments/retrieval.zip

      - name: Deploy Visualisation Lambda
        run: |
          cd visualisation
          pip install -r requirements.txt -t package/
          cp handler.py package/
          cd package && zip -r ../deployment.zip .
          cd ..
          aws s3 cp deployment.zip s3://${{ secrets.AWS_BUCKET_NAME }}/prod/deployments/visualisation.zip
          aws lambda update-function-code \
            --function-name bravo-visualisation-prod \
            --s3-bucket ${{ secrets.AWS_BUCKET_NAME }} \
            --s3-key prod/deployments/visualisation.zip
